conda activate silkjam

run() {
    if [ "$1" = "--debug" ]; then
        ARGS='--build-arg DEBUG=true'
        # echo "Running with DEBUG"
    fi
    docker compose stop silkjam
    docker compose build $ARGS
    docker compose up -d
}